# Agentic System MVP 🤖

Автономная многоагентная система с интерфейсом в Telegram, способная выполнять команды, искать информацию в сети и управлять собственной долговременной памятью.

## 🚀 Основные возможности

- **Telegram Interface:** Полноценное взаимодействие через бота с индикацией прогресса ("печатает", "думает", выполнение инструментов").
- **Actor-Based Multi-Agent Architecture:**
  - **Actor Agent:** Основной агент, реализованный с использованием Actor Framework, который создает подчиненные акторы для выполнения инструментов.
  - **Context Agent:** Анализирует диалоги и архивирует устаревшие темы для экономии контекста.
  - **Safety Agent:** Проверяет команды на безопасность перед выполнением.
- **Инструментарий (Tools), реализованный через акторы:**
  - `run_command`: Выполнение системных команд (bash/powershell) через CommandActor.
  - `web_search`: Поиск актуальной информации через DuckDuckGo через WebSearchActor.
- **Управление памятью:**
  - `soul.md`: Определение личности и поведения агента.
  - `user.md`: Знания о пользователе.
  - **Архивация:** Автоматическое суммаризирование и сохранение завершенных обсуждений в `archives/`.
- **Гибкость LLM:** Поддержка OpenAI и OpenRouter (по умолчанию используется OpenRouter для доступа к широкому спектру моделей).
- **Actor Framework:** Кастомная реализация системы акторов для изоляции выполнения инструментов и масштабируемости.

## 🛠 Технологический стек

- **Язык:** Python 3.11+
- **Фреймворк бота:** `python-telegram-bot` (v20+)
- **Асинхронность:** `asyncio`, `aiofiles`, `aiohttp`
- **LLM Провайдеры:** OpenRouter, OpenAI
- **Хранилище:** JSON-файлы с блокировками (`filelock`) для обеспечения целостности данных.
- **Логирование:** Структурированные логи (`structlog`, `loguru`).

## 📁 Структура проекта

- `src/agents/`: Реализация логики агентов и их поведения.
- `src/telegram/`: Код Telegram-бота и обработчиков.
- `src/session/`: Управление сессиями и персистентностью пользователей.
- `src/llm/`: Абстракции и провайдеры для работы с различными AI моделями.
- `src/executor/`: Модули для выполнения внешних задач (команды, поиск).
- `data/`: Хранилище сессий, истории (`history.jsonl`) и личной памяти пользователей.
- `context/`: Системные промпты и базовые шаблоны памяти.

## ⚙️ Установка и запуск

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository_url>
   cd agents
   ```

2. **Настройте окружение:**
   - Скопируйте `.env.example` в `.env`.
   - Укажите ваши API ключи (`TELEGRAM_BOT_TOKEN`, `OPENROUTER_API_KEY`).

3. **Установите зависимости:**
   ```bash
   pip install -e .
   ```

4. **Запустите систему:**
   ```bash
   python -m src.main
   ```

## 🧪 Тестирование

Запуск тестов осуществляется через `pytest`:
```bash
pytest
```

## 🛡 Безопасность

Все системные команды проходят проверку через `SafetyAgent`. По умолчанию разрешены безопасные команды. Настройка разрешенных паттернов производится в `src/agents/safety_agent.py` или через `.env`.
